// Write a function that takes 1 parameter

// In that function, count from 1 to that 
// parameter, printing out the number

// That is all.

function count(num) {
    for(var i = 0; i <= num; i++) {
        console.log(i);
    }
}

count(10);
count(4);
count(15);

